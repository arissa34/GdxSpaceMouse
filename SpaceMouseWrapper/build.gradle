plugins {
    id 'java-library'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

jar { // for building the spacemouse fat jar with native lib inside
    into('libs') {
        from 'libs'
    }
}

